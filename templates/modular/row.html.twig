<div class="container">
    <div class="row">
        {% for item in page.header.columns %}
            <div class="{{ item.col ?: 'col'}}">
                <h2>{{ item.content }}</h2>
                
                {{ item.content }}
            </div> 
        {% endfor %}
    </div>
</div>