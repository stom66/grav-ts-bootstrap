title: Columns
'@extends':
    type: default
    context: blueprints://pages

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        gallery:
          type: tab
          title: Columns

          fields:
            header.columns:
              name: Columns
              type: list
              style: vertical
              label: Columns
              fields:
                .title:
                  type: text
                  label: Title
                .content:
                  type: markdown
                  label: Content
                .class:
                  type: text
                  label: Class (defaults to col)